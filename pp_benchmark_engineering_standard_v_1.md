# Pure-Pursuit Benchmark 工程标准文档（Final Standard / Freeze Version）

**文档状态**：冻结版本（Final Standard）  
**文档用途**：作为本项目唯一工程标准、实现边界与验收基线  
**适用范围**：ROS2 / Nav2 下 pure-pursuit 算法族场景化 benchmark 工程  
**约束级别**：高  

---

## 1. 文档地位

本文档是本项目的**最终工程标准文档**。自本文档确认后，项目的目标、范围、系统结构、benchmark 场景、评估指标、模块职责、非目标项与收敛边界均以本文档为准。

后续任何实现、重构、扩展、删减、文档改写、Codex 自动修改，都必须以本文档作为**对照标准**。

### 1.1 解释优先级

若出现以下冲突，优先级如下：

1. 本文档
2. 当前工程中与本文档一致的正式 benchmark 主链实现
3. 旧文档、旧注释、历史入口、临时脚本、演示链代码中的遗留表述

### 1.2 约束原则

- **凡是不符合本文档定义的实现，视为偏离。**
- **凡是超出本文档边界的功能扩张，视为未经批准的范围蔓延。**
- **凡是与本文档冲突的旧入口、旧参数、旧表述，应被移除、废弃或降级。**

---

## 2. 项目最终定义

本项目的最终定义如下：

> 本项目是一个基于 ROS2/Nav2 的 pure-pursuit 算法族场景化 benchmark 工程。该工程围绕高曲率路径、狭窄通道以及两者耦合场景，统一运行不同 pure-pursuit 控制器，统一采集实验数据，统一计算评估指标，统一输出对比结果，用于评判 pure-pursuit 算法族在不同典型几何约束与空间约束下的优劣。

### 2.1 项目本质

本项目本质上是：

- 一个**benchmark 工程项目**
- 一个**局部路径跟踪控制器评测工程**
- 一个围绕 **pure-pursuit 算法族** 的**场景化自动评测系统**

本项目**不是**：

- 新算法发明项目
- 纯论文导向研究框架
- 单次 demo 演示代码集合
- 仅用于调试的 pre-benchmark skeleton
- 仅复现官方急转弯示例的复刻项目

### 2.2 项目最终交付形态

本项目最终应呈现为：

1. 一套正式 benchmark 主链  
2. 一组标准化 benchmark 场景  
3. 一组标准化 controller profiles  
4. 一套自动化日志采集与离线指标评估链  
5. 一套辅助性的人工演示 / 可视化 / debug 工具链  
6. 一组 pure-pursuit 算法族在典型场景下的对比结果与结论输出

---

## 3. 项目目标

### 3.1 总目标

构建一个可运行、可复现、可扩展、可评估的 pure-pursuit benchmark 工程，在统一执行框架下，对 pure-pursuit 算法族在典型导航场景中的性能差异进行工程化对比。

### 3.2 具体目标

项目必须完成以下目标：

1. 在 **高曲率路径场景** 下，对 pure-pursuit 算法族的路径跟踪性能进行对比。  
2. 在 **狭窄通道场景** 下，对 pure-pursuit 算法族的通行安全性进行对比。  
3. 在 **高曲率 + 狭窄通道耦合场景** 下，对 pure-pursuit 算法族的鲁棒性与失效模式进行对比。  
4. 通过统一的 benchmark 主链，实现 controller 切换、场景执行、日志采集、指标计算与结果输出。  
5. 形成可供答辩、展示、复现实验使用的标准工程交付物。

---

## 4. 项目边界

### 4.1 项目范围内

以下内容属于本项目正式范围：

- ROS2 / Nav2 下 pure-pursuit 算法族 benchmark 执行
- benchmark 场景配置与场景分类
- controller profiles 统一管理
- benchmark_mode 下 supervisor 安全门控
- 运行过程监控与日志采集
- 离线 metrics 评估与结果汇总
- benchmark 结果可视化与对比
- 人工演示 / debug / integration 辅助链

### 4.2 项目范围外

以下内容不属于本项目主范围：

- 提出新的纯追踪控制算法
- 重构 Nav2 核心架构
- 替换官方 controller server 体系
- 构建完整通用导航平台产品
- 为 GUI 重写复杂 Qt 前端
- 追求通用机器人全栈 benchmark 平台化
- 对所有导航算法家族进行全面横向对比

### 4.3 明确非目标

本项目不以“提出新算法”为目标；不以“构建工业级通用平台”为目标；不以“覆盖所有动态复杂交通场景”为目标。

本项目的目标是**做成一个纯追踪算法族的场景化 benchmark 工程**，并形成清晰、可解释、可复现的对比结果。

---

## 5. 官方基线与本项目扩展关系

### 5.1 官方基线的定位

官方已有内容主要体现为：

- 针对 pure-pursuit / adaptive pure-pursuit / regulated pure-pursuit 的基础演示
- 偏向于**高曲率 / 急转弯微型 benchmark** 的受控示例

该官方基线可作为本项目的**高曲率 benchmark 参考起点**。

### 5.2 官方基线的局限

官方急转弯 micro-benchmark 只能较好体现：

- 曲率变化加大时 controller 的跟踪退化
- 急转弯路径下的轨迹偏移、切角、超调等问题

但不足以独立覆盖：

- 狭窄通道中的安全性差异
- 障碍物距离约束下的通行行为差异
- 高曲率与狭窄空间共同作用时的耦合效应

### 5.3 本项目的正式扩展

本项目在官方基线基础上，正式扩展出以下两条能力线：

1. **狭窄通道跟踪安全 benchmark**  
2. **高曲率 + 狭窄通道耦合 stress benchmark**

因此，本项目最终不是“只复现官方急转弯 benchmark”，而是形成：

- 官方高曲率基线保留
- 狭窄通道场景新增
- 耦合极端场景新增
- 统一 benchmark 主链输出

---

## 6. 最终 benchmark 体系

本项目的正式 benchmark 体系固定为以下三类。

### 6.1 Benchmark A：高曲率路径跟踪性能

#### 目标
评估 pure-pursuit 算法族在高曲率路径条件下的跟踪精度、轨迹稳定性与转弯性能。

#### 关注点
- 跟踪是否准确
- 转弯是否平稳
- 是否出现明显超调或切角
- 速度保持是否合理

#### 典型场景
- 45° 转角路径
- 90° 转角路径
- 135° 转角路径
- 平滑圆角/样条化高曲率路径

#### 结果定位
该 benchmark 是**正式主结果之一**。

---

### 6.2 Benchmark B：狭窄通道跟踪安全性

#### 目标
评估 pure-pursuit 算法族在狭窄通道中通行时的安全性、障碍物余量、贴边行为与通过能力。

#### 关注点
- 是否发生擦碰或碰撞
- 最小障碍物距离是否足够
- 轨迹是否出现贴墙振荡
- 在受限空间中的通过稳定性如何

#### 典型场景
- 宽松通道
- 中等通道
- 紧凑通道
- 入口收缩 / 直通道 / 有限转角通道

#### 结果定位
该 benchmark 是**正式主结果之一**。

---

### 6.3 Benchmark C：高曲率 + 狭窄通道耦合场景

#### 目标
评估 pure-pursuit 算法族在几何约束和空间约束共同作用下的鲁棒性、退化行为与失效模式。

#### 关注点
- 急转弯与狭窄空间叠加后是否仍能稳定通过
- 是否出现角点切入障碍物
- 是否需要明显降速或停滞
- 哪类 controller 在极端条件下更鲁棒

#### 典型场景
- 窄通道中的高曲率转弯
- 近障碍物急转弯
- 转角与通道收缩耦合场景

#### 结果定位
该 benchmark 是**正式主结果之一**，同时也是**stress / interaction benchmark**。

---

### 6.4 动态障碍场景

动态障碍场景可作为扩展 benchmark 保留，但不构成项目最核心结果。

其定位为：

- 扩展实验
- 可选增强项
- 时间或资源不足时可降级到附录/补充说明

其存在不应挤占 A/B/C 三类 benchmark 的主结果地位。

---

## 7. 被评测对象

本项目的核心评测对象是 **pure-pursuit 算法族**。

### 7.1 被评测对象类别

至少包括：

- Classical Pure Pursuit（PP）
- Adaptive Pure Pursuit（APP）
- Regulated Pure Pursuit（RPP）
- 工程中定义的第 4 个 benchmark controller profile（若为参数变体，则明确作为 profile 变体而非新算法）

### 7.2 对比原则

所有被评测 controller 必须在统一框架下进行对比：

- 使用同一机器人模型
- 使用同一仿真基础环境
- 使用同一场景生成逻辑
- 使用同一日志链
- 使用同一 metrics 计算方法
- 使用统一的 benchmark_mode 安全门控

### 7.3 禁止事项

禁止出现以下情况：

- 同一 benchmark 中不同 controller 使用不同场景定义
- 不同 controller 之间存在隐藏的默认参数差异
- 旧入口和新入口调用不同 profile 默认值，导致对比失真
- 将 demo 参数误当成 benchmark 正式参数

---

## 8. 指标体系

项目的正式指标体系分为三类：跟踪类指标、安全类指标、效率/鲁棒性类指标。

### 8.1 跟踪类指标

- 平均横向误差
- 最大横向误差
- 平均路径跟踪误差
- 最大超调
- 航向偏差（如已实现）
- 转弯完成时间
- 角速度波动 / 平滑性（如已实现）

### 8.2 安全类指标

- 最小障碍物距离
- 是否发生擦碰
- 是否发生碰撞
- 是否越界 / 侵入障碍区域
- 通道通过成功率

### 8.3 效率 / 鲁棒性类指标

- 平均速度
- 通过时间
- 成功率
- 停滞次数 / 超时次数
- 重试或中断次数（如已实现）
- 典型失效模式分类（如已实现）

### 8.4 benchmark 对应指标映射

#### Benchmark A：高曲率路径跟踪性能
重点指标：
- 平均跟踪误差
- 最大误差
- 最大超调
- 转弯耗时
- 平均速度 / 速度保持能力

#### Benchmark B：狭窄通道跟踪安全性
重点指标：
- 最小障碍物距离
- 横向偏移
- 是否擦碰/碰撞
- 平均速度
- 成功率

#### Benchmark C：高曲率 + 狭窄通道耦合场景
重点指标：
- 成功率
- 碰撞率
- 最小安全间距
- 通过时间
- 停滞/失效次数
- 典型失效模式

---

## 9. 系统架构与模块职责

本项目的系统架构固定为：

1. **正式 benchmark 主链**  
2. **辅助 demo / debug 链**

两者必须职责分离，不得混淆。

---

### 9.1 正式 benchmark 主链

正式 benchmark 主链是本项目最核心的工程骨架，用于生成正式结果。

#### 核心组成
- `evaluation.launch.py`
- `monitor_logger`
- `evaluation_metrics`
- 场景化 benchmark 入口
- controller profiles
- supervisor 的 benchmark_mode 安全门控

#### 职责
- 启动正式 benchmark 执行流程
- 装载场景与 controller profile
- 运行仿真/控制执行
- 采集运行过程数据
- 输出日志与评估所需记录
- 触发或支撑离线 metrics 计算
- 形成正式结果来源

#### 身份定义
这是**唯一正式 benchmark backbone**。

凡正式论文、答辩、报告、结论、图表、指标统计，都应以该主链结果为准。

---

### 9.2 辅助 demo / debug 链

辅助链保留，但必须严格降级定位。

#### 核心组成
- `benchmark_demo.launch.py`
- `benchmark_gui`
- `benchmark_visualizer`

#### 职责
- 人工触发 controller 运行
- 路径/轨迹可视化观察
- integration 流程检查
- debug 与展示辅助

#### 禁止承担的职责
辅助链不得被定义为：

- 正式 benchmark 结果来源
- 正式指标统计主链
- 正式论文结论基线

#### 身份定义
这是**人工演示 / 调试 / 展示工具链**，不是正式 benchmark 主链。

---

## 10. 入口与参数收敛标准

### 10.1 入口收敛

本项目最终只承认两类入口：

#### 正式入口
- `evaluation.launch.py`

#### 非正式入口
- `benchmark_demo.launch.py`

除上述两类入口外，其他历史入口、临时入口、重复 launch、旧测试入口，如其功能已被覆盖，应删除、废弃或移出主流程。

### 10.2 参数真源

本项目必须存在且仅存在一个 benchmark 默认参数真源（single source of truth）。

以下内容必须从该统一参数源读取或派生：

- controller profiles 默认参数
- scenario 配置默认参数
- supervisor benchmark_mode 默认值
- 正式 benchmark 执行所需关键默认值

### 10.3 禁止重复默认值

以下内容必须删除或废弃：

- demo GUI 中写死的 benchmark 默认参数
- 旧入口保留的另一套 profile 默认值
- 与正式 benchmark 配置冲突的历史默认值
- 新旧入口并行维护的重复参数源

### 10.4 参数覆盖规则

允许 demo/debug 场景临时覆盖参数，但必须满足：

- 覆盖行为显式可见
- 不改变正式 benchmark 默认真源
- 不被误用为正式实验基线

---

## 11. 场景设计标准

### 11.1 场景设计原则

本项目场景必须围绕 benchmark 目的服务，场景设计要满足：

- 可重复
- 可配置
- 可区分不同 controller 行为
- 可产生可解释的指标差异

### 11.2 高曲率场景原则

高曲率场景用于测跟踪性能，不应被狭窄障碍物主导。

因此：

- 应允许观察 controller 的轨迹偏差、切角、超调等现象
- 不应把碰撞作为该类 benchmark 的唯一判定
- 应尽量避免将“不可跟踪的尖锐折线”作为唯一正式路径表达
- 更推荐平滑圆角、样条化转角或可控曲率路径

### 11.3 狭窄通道场景原则

狭窄通道场景用于测安全性，不应让极端高曲率成为唯一难点。

因此：

- 通道宽度应可配置
- 障碍物余量应可量化
- 应能观察最小障碍物距离、擦碰、贴墙振荡、是否成功通过等行为

### 11.4 耦合场景原则

耦合场景用于测复合约束下的鲁棒性与失效模式。

因此：

- 必须同时存在明显转弯难度与空间压缩难度
- 应突出 interaction/stress 特征
- 应允许识别不同 controller 的退化方式

---

## 12. 工程收敛原则

### 12.1 收敛方向

本项目后续工程修改的原则不是持续扩平台，而是：

- 明确正式主链
- 降级演示链
- 合并重复入口
- 合并重复参数源
- 删除历史冗余
- 围绕 A/B/C benchmark 结果收敛

### 12.2 不再追求的方向

后续不应把主要精力投入到以下方向：

- GUI 美化
- Qt 重构
- 非必要的交互前端增强
- 平台化过度抽象
- 与 benchmark 主目标无关的功能扩展

### 12.3 允许的修改类型

仅允许以下类型的修改作为主线工作：

- 场景收敛与命名规范化
- 正式主链稳定性增强
- controller profile 统一化
- metrics 计算补全与修正
- 日志链稳定化
- 历史冗余删除
- 结果输出标准化

---

## 13. Codex 执行标准

本节用于约束后续 Codex 对工程的修改方向。

### 13.1 Codex 必须遵循的总原则

Codex 的一切改动，都必须对照本文档执行。

Codex 的职责不是重新定义项目，而是：

- 使工程更加接近本文档定义的最终形态
- 删除与本文档冲突的历史包袱
- 防止项目继续发散

### 13.2 Codex 必须做的事

Codex 在后续迭代中应优先执行：

1. 正式 benchmark 主链收敛  
2. demo/debug 链降级定位  
3. 重复入口删除  
4. 重复参数删除  
5. 冗余脚本删除  
6. 场景分类与命名收敛  
7. A/B/C benchmark 体系对齐  
8. metrics 与日志主链对齐  
9. 文档与注释中的错误定位修正

### 13.3 Codex 禁止做的事

Codex 不得：

- 擅自扩张项目目标
- 把项目重写为通用平台
- 把 demo 链提升为正式结果链
- 为 GUI 重写复杂系统
- 引入与 benchmark 主目标无关的大规模重构
- 保留多个相互冲突的默认参数源
- 继续让旧入口与新入口并行承担同类职责

### 13.4 偏离判定

若 Codex 的实现出现以下情况，则视为偏离本文档：

- benchmark 场景方向从 A/B/C 主线偏移
- 正式主链与演示链职责再次混淆
- 参数真源再次分裂
- 历史入口继续保留且参与主流程
- 结果输出仍依赖 demo 工具链
- 项目被重新表述为 pre-benchmark skeleton 或临时演示工程

---

## 14. 冗余内容删除标准

后续工程整理中，应优先删除以下冗余内容：

### 14.1 冗余入口
- 被正式入口或 demo 入口覆盖的历史 launch 文件
- 旧测试入口
- 临时验证入口
- 重复承担同类职责的脚本

### 14.2 冗余参数
- 历史默认参数
- 与 benchmark profile 冲突的旧参数
- demo 中写死的正式参数副本
- 多处维护的相同 controller 默认值

### 14.3 冗余脚本
- 不参与正式 benchmark 数据输出的历史耦合脚本
- 仅为旧演示链服务但已无必要的胶水脚本
- 主流程已经覆盖后仍遗留的调试脚本

### 14.4 冗余表述
- “平台整体更适合 pre-benchmark”
- “不能直接做正式算法结论”
- “还不是成熟 benchmark 平台”
- “demo 链是主流程之一”

这些表述应改为：

- 正式 benchmark 主链已可用于场景化评测
- demo/debug 链主要用于调试和交互验证
- 工程范围聚焦于 pure-pursuit 场景化 benchmark

---

## 15. 验收标准

本项目达到最终验收时，至少应满足以下条件：

### 15.1 结构验收
- 正式 benchmark 主链明确存在并稳定可用
- demo/debug 链定位清晰且不混入正式结果
- 入口已收敛
- 参数真源已收敛
- 主要历史冗余已清理

### 15.2 功能验收
- 可切换多个 pure-pursuit controller profiles
- 可执行 A/B/C 三类 benchmark 场景
- 可完成运行日志采集
- 可完成离线指标计算
- 可输出 controller 对比结果

### 15.3 结果验收
- 高曲率 benchmark 有正式结果
- 狭窄通道 benchmark 有正式结果
- 耦合场景 benchmark 有正式结果
- 结果可用于答辩展示、报告撰写与结论说明

---

## 16. 项目最终表述模板

在任何对外或对内正式描述中，本项目应优先使用以下表述：

> 本项目是一个基于 ROS2/Nav2 的 pure-pursuit 算法族场景化 benchmark 工程。项目在官方高曲率基线基础上，扩展了狭窄通道安全 benchmark 与高曲率-狭窄通道耦合 benchmark，通过统一的 benchmark 主链对不同 pure-pursuit 控制器进行统一执行、统一采集、统一评估，并输出可比较的性能结果。

简化表述可使用：

> 本项目是一个面向 pure-pursuit 算法族的场景化 benchmark 工程，重点评估其在高曲率、狭窄通道及耦合场景下的性能差异。

---

## 17. 冻结结论

本文档确认以下结论为项目最终结论，不再反复摇摆：

1. 本项目的本质是 **benchmark 工程项目**，不是新算法项目。  
2. 官方急转弯内容仅构成**高曲率基线**，不足以覆盖项目全部目标。  
3. 项目的正式主结果必须覆盖：  
   - 高曲率路径跟踪性能  
   - 狭窄通道跟踪安全性  
   - 高曲率 + 狭窄通道耦合场景鲁棒性  
4. 正式 benchmark 主链是项目核心；demo/debug 链是辅助。  
5. 项目后续迭代应以收敛和清理为主，而非继续发散扩张。  
6. Codex 后续所有修改必须以本文档为唯一标准进行对齐。

---

## 18. 最终一句话定义

> 这是一个基于 ROS2/Nav2 的 pure-pursuit 算法族场景化 benchmark 工程，最终以高曲率、狭窄通道及其耦合场景为核心评测体系，通过统一执行、统一采集、统一评估的正式主链，对 pure-pursuit 家族控制器的性能优劣进行工程化对比与输出。

